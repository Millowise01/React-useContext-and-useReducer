// Centralized theme name constants.
// Keeping these in one place means no other file has to hardcode
// the literal strings "light" / "dark" — if a theme name ever changes,
// it only needs to change here.

export const LIGHT_THEME = "light";
export const DARK_THEME = "dark";